import { ScrollableDirectiveDirective } from './scrollable-directive.directive';

describe('ScrollableDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollableDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
